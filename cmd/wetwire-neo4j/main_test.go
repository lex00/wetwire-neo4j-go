package main

import (
	"testing"
)

// Tests for list and graph commands have been moved to domain package
// These commands are now provided by the wetwire-core-go domain interface
func TestCommandsMigrated(t *testing.T) {
	t.Log("list and graph commands are now provided by the domain interface")
}
